import "./globals.css";
export const metadata = { title: "Acquisition POC Dashboard — StayVista", description: "Daily performance tracking" };
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
